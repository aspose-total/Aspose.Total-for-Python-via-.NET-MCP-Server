# Aspose HTML MCP Server — runtime dependencies
# Create venv:  python -m venv .venv
# Install:      .venv\Scripts\pip install -r requirements.txt  (Windows)
#               .venv/bin/pip install -r requirements.txt      (Linux/Mac)

mcp>=1.0.0
fastmcp>=2.0.0
python-dotenv>=1.0.0
pyyaml>=6.0.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Aspose — note: package name is aspose-html-net (NOT aspose-html)
aspose-html-net
